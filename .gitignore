# Default is to whitelist all files, except those listed. Alter
# Blacklist files/folders in same directory as the .gitignore file
# /*


# Whitelist some files
!.gitignore
!README.md

# Ignore all files named .DS_Store or ending with .log or .swp
**/.DS_Store
**.log
**.swp

# Examples for including:
#
# Whitelist folder/a/b1/ and folder/a/b2/
# trailing "/" is optional for folders, may match file though.
# "/" is NOT optional when followed by a *
# !folder/
# folder/*
# !folder/a/
# folder/a/*
# !folder/a/b1/
# !folder/a/b2/
# !folder/a/file.txt
#
# Adding to the above, this also works...
# !/folder/a/deeply
# /folder/a/deeply/*
# !/folder/a/deeply/nested
# /folder/a/deeply/nested/*
# !/folder/a/deeply/nested/subfolder

# Files already in git tree generated by running
#   home ls-tree -r master --name-only

# Ignore 
/.zcomp*
/.*_history
/.wget-hsts
/.CFUserTextEncoding
/.fontconfig/
/.dvdcss/
/.local/
/.cups/

# Ignore ssh key files, but not the config/authorized keys
/.ssh/
!/.ssh/config
!/.ssh/authorized_keys

# Ignore gpg except the config file
/.gnupg
!/.gnupg/gpg.conf

# Include ~/.profile.d login files
!/.profile.d

# Include git config, but ignore my repos
/git
!/.gitconfig

# Special directories to ignore
/Applications/
/Desktop/
/Documents/
/Downloads/
/Dropbox/
/Google Drive/
/Library/
/Movies/
/Music/
/Pictures/
/Mail/
/Public/
/Sites/
/src/
/tmp/
/.Trash/
/Virtual Machines.localized/
/VirtualBox VMs/

# Specific files to ignore
.rnd
.lesshst
.pearrc
.viminfo
.yarnrc
.Brewfile.lock.json
.aws/credentials

# Hidden directories to ignore
/.bundle/
/.composer/
/.cache/
/.cpan/
/.cups/
/.docker/
/.dropbox/
/.gem/
/.mume/
/.ncftp/
/.node-gyp/
/.npm/
/.oh-my-zsh/
/.oracle_jre_usage/
/.pdepend/
/.pdk/
/.pdsh/
/.pgadmin/
/.phpls/
/.pylint.d/
/.python-eggs/
/.r10k/
/.soapuios/
/.terraform.d
/.vnc/
/.vscode-react-native/
/.vscode/
/.zenmap/
/.iterm2/

/.links
!/.links/bookmarks.html

# If I were blacklisting all files, I'd have to add every file already in the
# git tree; generated by running
#   home ls-tree -r master --name-only | prepend '!/'
#
# (see ~/.profile for the 'prepend' function)
# !/.bashrc
# !/.gitconfig
# !/.gitignore
# !/.gvimrc
# !/.home/README.md
# !/.iterm2_shell_integration.bash
# !/.iterm2_shell_integration.zsh
# !/.profile
# !/.profile.d/.agnoster.bash
# !/.ssh/authorized_keys
# !/.ssh/config
# !/.tmux.conf
# !/.vim/syntax/ios.vim
# !/.vimrc
# !/.zshrc
